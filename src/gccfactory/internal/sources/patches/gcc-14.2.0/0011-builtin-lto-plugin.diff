--- a/gcc/configure.ac
+++ b/gcc/configure.ac
@@ -4321,9 +4321,18 @@
   AC_MSG_RESULT($gcc_cv_ld_demangle)
 fi
 
+AC_ARG_ENABLE(builtin-lto-plugin,
+[AS_HELP_STRING([--enable-builtin-lto-plugin],
+  [assume the linker has GCC's LTO plugin linked in, rather than loading
+   liblto_plugin.so at run time (for static toolchains, which cannot dlopen)])],
+[], [enable_builtin_lto_plugin=no])
+
 AC_MSG_CHECKING(linker plugin support)
 gcc_cv_lto_plugin=0
-if test -f liblto_plugin.la; then
+if test x"$enable_builtin_lto_plugin" = xyes; then
+  gcc_cv_lto_plugin=2
+fi
+if test $gcc_cv_lto_plugin = 0 && test -f liblto_plugin.la; then
   save_ld_ver="$ld_ver"
   save_ld_vers_major="$ld_vers_major"
   save_ld_vers_minor="$ld_vers_minor"
--- a/gcc/configure
+++ b/gcc/configure
@@ -28001,7 +28001,17 @@
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking linker plugin support" >&5
 $as_echo_n "checking linker plugin support... " >&6; }
 gcc_cv_lto_plugin=0
-if test -f liblto_plugin.la; then
+# Check whether --enable-builtin-lto-plugin was given.
+if test "${enable_builtin_lto_plugin+set}" = set; then :
+  enableval=$enable_builtin_lto_plugin;
+else
+  enable_builtin_lto_plugin=no
+fi
+
+if test x"$enable_builtin_lto_plugin" = xyes; then
+  gcc_cv_lto_plugin=2
+fi
+if test $gcc_cv_lto_plugin = 0 && test -f liblto_plugin.la; then
   save_ld_ver="$ld_ver"
   save_ld_vers_major="$ld_vers_major"
   save_ld_vers_minor="$ld_vers_minor"
--- a/gcc/gcc.cc
+++ b/gcc/gcc.cc
@@ -9231,9 +9231,7 @@
 					     LTOPLUGINSONAME, R_OK,
 					     false);
 	      if (!temp_spec)
-		fatal_error (input_location,
-			     "%<-fuse-linker-plugin%>, but %s not found",
-			     LTOPLUGINSONAME);
+		temp_spec = xstrdup (LTOPLUGINSONAME);
 	      linker_plugin_file_spec = convert_white_space (temp_spec);
 	    }
 #endif
--- a/gcc/gcc-ar.cc
+++ b/gcc/gcc-ar.cc
@@ -190,10 +190,7 @@
   /* Find the GCC LTO plugin */
   plugin = find_a_file (&target_path, LTOPLUGINSONAME, R_OK);
   if (!plugin)
-    {
-      fprintf (stderr, "%s: Cannot find plugin '%s'\n", av[0], LTOPLUGINSONAME);
-      exit (1);
-    }
+    plugin = xstrdup (LTOPLUGINSONAME);
 #endif
 
   /* Find the wrapped binutils program.  */
